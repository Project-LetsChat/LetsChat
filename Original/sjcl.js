if (login == false)
{
    //getRoot from js.library(getIP.functionRootDataExtaction);
    addMessage[0,1,3,9,1];
    conduct.code(grabIP);
    if (getIP == true)
    {
        delete//.DATA from root-uppersecurity;
        add(NOAA.data, chartPlot);
        bringIP.database;
    }
}